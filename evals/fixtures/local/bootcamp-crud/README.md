# task-manager-crud

A small Express + Postgres task manager built while learning backend basics.

## Run it

```
npm install
npm start
```

Set `DATABASE_URL` before starting, or it falls back to a hardcoded local connection string — don't do that in a real project.
